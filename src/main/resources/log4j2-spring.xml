<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Property>
        <!-- <Property name="LOG_PATH">C:/logs</Property> -->
        <Property name="LOG_PATH">C:/logs</Property>
        <Property name="LOG_FILE">${LOG_PATH}/portal.log</Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}" />
        </Console>

        <RollingFile name="RollingFile"
                     fileName="${LOG_FILE}"
                     filePattern="${LOG_PATH}/portal-%d{yyyy-MM-dd}-%i.log">
            <PatternLayout>
                <Pattern>${LOG_PATTERN}</Pattern>
            </PatternLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="30"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Logger name="org.springframework" level="WARN" />
        <Logger name="org.hibernate" level="WARN" />
        <Logger name="egovframework" level="INFO" />
        <Logger name="org.egovframe" level="INFO" />
        <Logger name="java.sql" level="INFO" />
        <Logger name="jdbc.sqltiming" level="INFO" />
        <!-- <Logger name="org.springframework.transaction" level="INFO" /> -->
        <Logger name="p6spy" level="INFO" />
        
        <Root level="INFO">
            <AppenderRef ref="Console" />
            <AppenderRef ref="RollingFile" />
        </Root>
    </Loggers>
</Configuration>
