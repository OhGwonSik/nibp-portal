<?xml version="1.0" encoding="UTF-8"?>
<!-- prod : 정책원운영서버- 정책원운영 DB -->
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- 운영 환경: 로그 패턴 (스택트레이스 포함) -->
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n%throwable</Property>
        <!-- MDC 패턴 (요청 추적용 - 필요시 활성화) -->
        <Property name="LOG_PATTERN_WITH_MDC">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] [%X{requestId}] %-5level %logger{36} - %msg%n%throwable</Property>
        <Property name="LOG_PATH">/app/logs</Property>
        <Property name="LOG_FILE">${LOG_PATH}/prod.log</Property>
    </Properties>

    <Appenders>
        <!-- 일반 로그 파일 (WARN 이상) -->
        <RollingFile name="RollingFile"
                     fileName="${LOG_FILE}"
                     filePattern="${LOG_PATH}/prod-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout>
                <Pattern>${LOG_PATTERN}</Pattern>
            </PatternLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="100 MB"/>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="30">
                <Delete basePath="${LOG_PATH}" maxDepth="1">
                    <IfFileName glob="prod-*.log.gz" />
                    <IfLastModified age="30d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

        <!-- Error 로그 파일 (ERROR 이상만) -->
        <RollingFile name="ErrorFile"
                     fileName="${LOG_PATH}/error.log"
                     filePattern="${LOG_PATH}/error-%d{yyyy-MM-dd}-%i.log.gz">
            <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout>
                <Pattern>${LOG_PATTERN}</Pattern>
            </PatternLayout>
            <Policies>
                <SizeBasedTriggeringPolicy size="50 MB"/>
                <TimeBasedTriggeringPolicy interval="1"/>
            </Policies>
            <DefaultRolloverStrategy max="60">
                <Delete basePath="${LOG_PATH}" maxDepth="1">
                    <IfFileName glob="error-*.log.gz" />
                    <IfLastModified age="60d" />
                </Delete>
            </DefaultRolloverStrategy>
        </RollingFile>

        <!-- 비동기 Appender (성능 향상, 로그 유실 방지) -->
        <Async name="AsyncRollingFile" bufferSize="512" blocking="false">
            <AppenderRef ref="RollingFile"/>
            <!-- 버퍼 가득 찰 경우 정책: WARN 이상은 유실 방지 -->
            <Filters>
                <ThresholdFilter level="ERROR" onMatch="ACCEPT" onMismatch="NEUTRAL"/>
            </Filters>
        </Async>

        <Async name="AsyncErrorFile" bufferSize="256" blocking="true">
            <AppenderRef ref="ErrorFile"/>
            <!-- ERROR는 절대 유실되지 않도록 blocking -->
        </Async>
    </Appenders>

    <Loggers>
        <!-- 운영 환경: WARN 레벨 로깅 (INFO는 제외, SQL은 완전 차단) -->

        <!-- Spring Framework: WARN -->
        <Logger name="org.springframework" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- Spring Security: WARN (보안 이벤트 모니터링) -->
        <Logger name="org.springframework.security" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
            <AppenderRef ref="AsyncErrorFile"/>
        </Logger>

        <!-- Spring Boot Actuator: WARN (헬스체크, 메트릭) -->
        <Logger name="org.springframework.boot.actuate" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- 트랜잭션 모니터링: WARN (SQL 제외, 트랜잭션 이슈만) -->
        <Logger name="org.springframework.transaction" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        <Logger name="org.springframework.orm" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- Hibernate/JPA: WARN으로 조정 -->
        <Logger name="org.hibernate" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        <Logger name="org.hibernate.SQL" level="OFF" additivity="false"/>
        <Logger name="org.hibernate.type" level="OFF" additivity="false"/>
        <Logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="OFF" additivity="false"/>

        <!-- eGovFramework: WARN으로 변경 -->
        <Logger name="egovframework" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        <Logger name="org.egovframe" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- SQL 로깅 완전 비활성화 -->
        <Logger name="java.sql" level="OFF" additivity="false"/>
        <Logger name="jdbc.sqltiming" level="OFF" additivity="false"/>
        <Logger name="jdbc.sqlonly" level="OFF" additivity="false"/>
        <Logger name="jdbc.audit" level="OFF" additivity="false"/>
        <Logger name="jdbc.resultset" level="OFF" additivity="false"/>
        <Logger name="jdbc.resultsettable" level="OFF" additivity="false"/>
        <Logger name="jdbc.connection" level="OFF" additivity="false"/>
        <Logger name="p6spy" level="OFF" additivity="false"/>
        <Logger name="log4jdbc" level="OFF" additivity="false"/>

        <!-- MyBatis: WARN으로 조정 (SQL은 별도 차단) -->
        <Logger name="org.mybatis" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>
        <Logger name="org.mybatis.spring.mapper" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- Apache 라이브러리: WARN으로 조정 -->
        <Logger name="org.apache" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- Tomcat: WARN -->
        <Logger name="org.apache.catalina" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- HTTP 요청 로깅: WARN (성능 이슈, 타임아웃 등) -->
        <!-- 26.01.29 수정 WARN->INFO -->
        <Logger name="org.springframework.web" level="INFO" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- 26.01.29 추가 -->
        <!-- REST Controller 로깅 -->
        <Logger name="org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping" level="INFO" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!-- 예외 처리 -->
        <Logger name="org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
            <AppenderRef ref="AsyncErrorFile"/>
        </Logger>

        <!-- 애플리케이션 로그: WARN (경고/에러만 기록, INFO는 제외) -->
        <!-- 26.01.29 수정 WARN->INFO -->
        <Logger name="egovframework.admin" level="INFO" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
            <AppenderRef ref="AsyncErrorFile"/>
        </Logger>
        <Logger name="egovframework.common" level="WARN" additivity="false">
            <AppenderRef ref="AsyncRollingFile"/>
        </Logger>

        <!--
            특정 비즈니스 로직 모니터링이 필요한 경우 개별 설정 가능
            예: 로그인 시도, 권한 변경, 중요 데이터 변경 등
            주의: INFO 레벨은 개인정보가 포함되지 않도록 주의 필요
        -->
        <!-- <Logger name="egovframework.admin.login" level="WARN"/> -->
        <!-- <Logger name="egovframework.admin.auth" level="WARN"/> -->

        <!-- Root Logger: WARN (기본 로그 레벨) -->
        <Root level="WARN">
            <AppenderRef ref="AsyncRollingFile"/>
            <AppenderRef ref="AsyncErrorFile"/>
        </Root>
    </Loggers>
</Configuration>
